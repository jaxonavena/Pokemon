Run `ruby game.rb` to play!

A CLI based version of a Pokemon battle. Currently not fully implemented to have all features, such as 2 player or multiple attacks per Pokemon, but you can technically play a full round on `Auto` mode. `Auto` mode is intended to eventually be single player vs a CPU opponent, but it currently is actually implemented as the dev ENV where two people can play against each other in the same CLI.

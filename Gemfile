# frozen_string_literal: true

# TEMPLATE v1

source "https://rubygems.org"

ruby File.read(File.expand_path(File.join(__dir__, ".ruby-version")), mode: "rb").chomp

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'awesome_print'
gem 'rake'
gem 'rspec'
gem 'tty'
gem 'activesupport'
gem 'pry'
gem 'binding_of_caller'
gem 'pry-nav'
gem 'pry-byebug'